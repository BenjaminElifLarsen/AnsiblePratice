praticing ansible

Trying to figure out how to work with docker containers with a non-docker host. All containers states they have an OS that is the same as the control node and pretty sure that is not true.